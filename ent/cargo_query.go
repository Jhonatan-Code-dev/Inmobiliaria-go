// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"rentals-go/ent/cargo"
	"rentals-go/ent/contrato"
	"rentals-go/ent/pagoaplicacion"
	"rentals-go/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CargoQuery is the builder for querying Cargo entities.
type CargoQuery struct {
	config
	ctx                  *QueryContext
	order                []cargo.OrderOption
	inters               []Interceptor
	predicates           []predicate.Cargo
	withContrato         *ContratoQuery
	withAplicacionesPago *PagoAplicacionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CargoQuery builder.
func (_q *CargoQuery) Where(ps ...predicate.Cargo) *CargoQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CargoQuery) Limit(limit int) *CargoQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CargoQuery) Offset(offset int) *CargoQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CargoQuery) Unique(unique bool) *CargoQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CargoQuery) Order(o ...cargo.OrderOption) *CargoQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryContrato chains the current query on the "contrato" edge.
func (_q *CargoQuery) QueryContrato() *ContratoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cargo.Table, cargo.FieldID, selector),
			sqlgraph.To(contrato.Table, contrato.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, cargo.ContratoTable, cargo.ContratoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAplicacionesPago chains the current query on the "aplicaciones_pago" edge.
func (_q *CargoQuery) QueryAplicacionesPago() *PagoAplicacionQuery {
	query := (&PagoAplicacionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(cargo.Table, cargo.FieldID, selector),
			sqlgraph.To(pagoaplicacion.Table, pagoaplicacion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, cargo.AplicacionesPagoTable, cargo.AplicacionesPagoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Cargo entity from the query.
// Returns a *NotFoundError when no Cargo was found.
func (_q *CargoQuery) First(ctx context.Context) (*Cargo, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cargo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CargoQuery) FirstX(ctx context.Context) *Cargo {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Cargo ID from the query.
// Returns a *NotFoundError when no Cargo ID was found.
func (_q *CargoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cargo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CargoQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Cargo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Cargo entity is found.
// Returns a *NotFoundError when no Cargo entities are found.
func (_q *CargoQuery) Only(ctx context.Context) (*Cargo, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cargo.Label}
	default:
		return nil, &NotSingularError{cargo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CargoQuery) OnlyX(ctx context.Context) *Cargo {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Cargo ID in the query.
// Returns a *NotSingularError when more than one Cargo ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CargoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cargo.Label}
	default:
		err = &NotSingularError{cargo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CargoQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Cargos.
func (_q *CargoQuery) All(ctx context.Context) ([]*Cargo, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Cargo, *CargoQuery]()
	return withInterceptors[[]*Cargo](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CargoQuery) AllX(ctx context.Context) []*Cargo {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Cargo IDs.
func (_q *CargoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(cargo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CargoQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CargoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CargoQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CargoQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CargoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CargoQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CargoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CargoQuery) Clone() *CargoQuery {
	if _q == nil {
		return nil
	}
	return &CargoQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]cargo.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.Cargo{}, _q.predicates...),
		withContrato:         _q.withContrato.Clone(),
		withAplicacionesPago: _q.withAplicacionesPago.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithContrato tells the query-builder to eager-load the nodes that are connected to
// the "contrato" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CargoQuery) WithContrato(opts ...func(*ContratoQuery)) *CargoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContrato = query
	return _q
}

// WithAplicacionesPago tells the query-builder to eager-load the nodes that are connected to
// the "aplicaciones_pago" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CargoQuery) WithAplicacionesPago(opts ...func(*PagoAplicacionQuery)) *CargoQuery {
	query := (&PagoAplicacionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAplicacionesPago = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Cargo.Query().
//		GroupBy(cargo.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CargoQuery) GroupBy(field string, fields ...string) *CargoGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CargoGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = cargo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.Cargo.Query().
//		Select(cargo.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *CargoQuery) Select(fields ...string) *CargoSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CargoSelect{CargoQuery: _q}
	sbuild.label = cargo.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CargoSelect configured with the given aggregations.
func (_q *CargoQuery) Aggregate(fns ...AggregateFunc) *CargoSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CargoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !cargo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CargoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Cargo, error) {
	var (
		nodes       = []*Cargo{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withContrato != nil,
			_q.withAplicacionesPago != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Cargo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Cargo{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withContrato; query != nil {
		if err := _q.loadContrato(ctx, query, nodes, nil,
			func(n *Cargo, e *Contrato) { n.Edges.Contrato = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAplicacionesPago; query != nil {
		if err := _q.loadAplicacionesPago(ctx, query, nodes,
			func(n *Cargo) { n.Edges.AplicacionesPago = []*PagoAplicacion{} },
			func(n *Cargo, e *PagoAplicacion) { n.Edges.AplicacionesPago = append(n.Edges.AplicacionesPago, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CargoQuery) loadContrato(ctx context.Context, query *ContratoQuery, nodes []*Cargo, init func(*Cargo), assign func(*Cargo, *Contrato)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Cargo)
	for i := range nodes {
		fk := nodes[i].ContratoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(contrato.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "contrato_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CargoQuery) loadAplicacionesPago(ctx context.Context, query *PagoAplicacionQuery, nodes []*Cargo, init func(*Cargo), assign func(*Cargo, *PagoAplicacion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Cargo)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pagoaplicacion.FieldCargoID)
	}
	query.Where(predicate.PagoAplicacion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(cargo.AplicacionesPagoColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CargoID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cargo_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CargoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CargoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cargo.Table, cargo.Columns, sqlgraph.NewFieldSpec(cargo.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cargo.FieldID)
		for i := range fields {
			if fields[i] != cargo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withContrato != nil {
			_spec.Node.AddColumnOnce(cargo.FieldContratoID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CargoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(cargo.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = cargo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CargoGroupBy is the group-by builder for Cargo entities.
type CargoGroupBy struct {
	selector
	build *CargoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CargoGroupBy) Aggregate(fns ...AggregateFunc) *CargoGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CargoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CargoQuery, *CargoGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CargoGroupBy) sqlScan(ctx context.Context, root *CargoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CargoSelect is the builder for selecting fields of Cargo entities.
type CargoSelect struct {
	*CargoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CargoSelect) Aggregate(fns ...AggregateFunc) *CargoSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CargoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CargoQuery, *CargoSelect](ctx, _s.CargoQuery, _s, _s.inters, v)
}

func (_s *CargoSelect) sqlScan(ctx context.Context, root *CargoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
