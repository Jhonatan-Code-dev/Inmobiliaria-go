// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"rentals-go/ent/cargo"
	"rentals-go/ent/cliente"
	"rentals-go/ent/contrato"
	"rentals-go/ent/empresa"
	"rentals-go/ent/pago"
	"rentals-go/ent/predicate"
	"rentals-go/ent/unidad"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ContratoQuery is the builder for querying Contrato entities.
type ContratoQuery struct {
	config
	ctx         *QueryContext
	order       []contrato.OrderOption
	inters      []Interceptor
	predicates  []predicate.Contrato
	withEmpresa *EmpresaQuery
	withCliente *ClienteQuery
	withUnidad  *UnidadQuery
	withCargos  *CargoQuery
	withPagos   *PagoQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContratoQuery builder.
func (_q *ContratoQuery) Where(ps ...predicate.Contrato) *ContratoQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ContratoQuery) Limit(limit int) *ContratoQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ContratoQuery) Offset(offset int) *ContratoQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ContratoQuery) Unique(unique bool) *ContratoQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ContratoQuery) Order(o ...contrato.OrderOption) *ContratoQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryEmpresa chains the current query on the "empresa" edge.
func (_q *ContratoQuery) QueryEmpresa() *EmpresaQuery {
	query := (&EmpresaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contrato.Table, contrato.FieldID, selector),
			sqlgraph.To(empresa.Table, empresa.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, contrato.EmpresaTable, contrato.EmpresaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCliente chains the current query on the "cliente" edge.
func (_q *ContratoQuery) QueryCliente() *ClienteQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contrato.Table, contrato.FieldID, selector),
			sqlgraph.To(cliente.Table, cliente.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, contrato.ClienteTable, contrato.ClienteColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUnidad chains the current query on the "unidad" edge.
func (_q *ContratoQuery) QueryUnidad() *UnidadQuery {
	query := (&UnidadClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contrato.Table, contrato.FieldID, selector),
			sqlgraph.To(unidad.Table, unidad.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, contrato.UnidadTable, contrato.UnidadColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCargos chains the current query on the "cargos" edge.
func (_q *ContratoQuery) QueryCargos() *CargoQuery {
	query := (&CargoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contrato.Table, contrato.FieldID, selector),
			sqlgraph.To(cargo.Table, cargo.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, contrato.CargosTable, contrato.CargosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPagos chains the current query on the "pagos" edge.
func (_q *ContratoQuery) QueryPagos() *PagoQuery {
	query := (&PagoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(contrato.Table, contrato.FieldID, selector),
			sqlgraph.To(pago.Table, pago.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, contrato.PagosTable, contrato.PagosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Contrato entity from the query.
// Returns a *NotFoundError when no Contrato was found.
func (_q *ContratoQuery) First(ctx context.Context) (*Contrato, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contrato.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ContratoQuery) FirstX(ctx context.Context) *Contrato {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Contrato ID from the query.
// Returns a *NotFoundError when no Contrato ID was found.
func (_q *ContratoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contrato.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ContratoQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Contrato entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Contrato entity is found.
// Returns a *NotFoundError when no Contrato entities are found.
func (_q *ContratoQuery) Only(ctx context.Context) (*Contrato, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contrato.Label}
	default:
		return nil, &NotSingularError{contrato.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ContratoQuery) OnlyX(ctx context.Context) *Contrato {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Contrato ID in the query.
// Returns a *NotSingularError when more than one Contrato ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ContratoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contrato.Label}
	default:
		err = &NotSingularError{contrato.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ContratoQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Contratos.
func (_q *ContratoQuery) All(ctx context.Context) ([]*Contrato, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Contrato, *ContratoQuery]()
	return withInterceptors[[]*Contrato](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ContratoQuery) AllX(ctx context.Context) []*Contrato {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Contrato IDs.
func (_q *ContratoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(contrato.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ContratoQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ContratoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ContratoQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ContratoQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ContratoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ContratoQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContratoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ContratoQuery) Clone() *ContratoQuery {
	if _q == nil {
		return nil
	}
	return &ContratoQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]contrato.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.Contrato{}, _q.predicates...),
		withEmpresa: _q.withEmpresa.Clone(),
		withCliente: _q.withCliente.Clone(),
		withUnidad:  _q.withUnidad.Clone(),
		withCargos:  _q.withCargos.Clone(),
		withPagos:   _q.withPagos.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithEmpresa tells the query-builder to eager-load the nodes that are connected to
// the "empresa" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContratoQuery) WithEmpresa(opts ...func(*EmpresaQuery)) *ContratoQuery {
	query := (&EmpresaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEmpresa = query
	return _q
}

// WithCliente tells the query-builder to eager-load the nodes that are connected to
// the "cliente" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContratoQuery) WithCliente(opts ...func(*ClienteQuery)) *ContratoQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCliente = query
	return _q
}

// WithUnidad tells the query-builder to eager-load the nodes that are connected to
// the "unidad" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContratoQuery) WithUnidad(opts ...func(*UnidadQuery)) *ContratoQuery {
	query := (&UnidadClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUnidad = query
	return _q
}

// WithCargos tells the query-builder to eager-load the nodes that are connected to
// the "cargos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContratoQuery) WithCargos(opts ...func(*CargoQuery)) *ContratoQuery {
	query := (&CargoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCargos = query
	return _q
}

// WithPagos tells the query-builder to eager-load the nodes that are connected to
// the "pagos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ContratoQuery) WithPagos(opts ...func(*PagoQuery)) *ContratoQuery {
	query := (&PagoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPagos = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Contrato.Query().
//		GroupBy(contrato.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ContratoQuery) GroupBy(field string, fields ...string) *ContratoGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContratoGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = contrato.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.Contrato.Query().
//		Select(contrato.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *ContratoQuery) Select(fields ...string) *ContratoSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ContratoSelect{ContratoQuery: _q}
	sbuild.label = contrato.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContratoSelect configured with the given aggregations.
func (_q *ContratoQuery) Aggregate(fns ...AggregateFunc) *ContratoSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ContratoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !contrato.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ContratoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Contrato, error) {
	var (
		nodes       = []*Contrato{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withEmpresa != nil,
			_q.withCliente != nil,
			_q.withUnidad != nil,
			_q.withCargos != nil,
			_q.withPagos != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Contrato).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Contrato{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withEmpresa; query != nil {
		if err := _q.loadEmpresa(ctx, query, nodes, nil,
			func(n *Contrato, e *Empresa) { n.Edges.Empresa = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCliente; query != nil {
		if err := _q.loadCliente(ctx, query, nodes, nil,
			func(n *Contrato, e *Cliente) { n.Edges.Cliente = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUnidad; query != nil {
		if err := _q.loadUnidad(ctx, query, nodes, nil,
			func(n *Contrato, e *Unidad) { n.Edges.Unidad = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCargos; query != nil {
		if err := _q.loadCargos(ctx, query, nodes,
			func(n *Contrato) { n.Edges.Cargos = []*Cargo{} },
			func(n *Contrato, e *Cargo) { n.Edges.Cargos = append(n.Edges.Cargos, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPagos; query != nil {
		if err := _q.loadPagos(ctx, query, nodes,
			func(n *Contrato) { n.Edges.Pagos = []*Pago{} },
			func(n *Contrato, e *Pago) { n.Edges.Pagos = append(n.Edges.Pagos, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ContratoQuery) loadEmpresa(ctx context.Context, query *EmpresaQuery, nodes []*Contrato, init func(*Contrato), assign func(*Contrato, *Empresa)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Contrato)
	for i := range nodes {
		fk := nodes[i].EmpresaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(empresa.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "empresa_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ContratoQuery) loadCliente(ctx context.Context, query *ClienteQuery, nodes []*Contrato, init func(*Contrato), assign func(*Contrato, *Cliente)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Contrato)
	for i := range nodes {
		fk := nodes[i].ClienteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(cliente.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "cliente_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ContratoQuery) loadUnidad(ctx context.Context, query *UnidadQuery, nodes []*Contrato, init func(*Contrato), assign func(*Contrato, *Unidad)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Contrato)
	for i := range nodes {
		fk := nodes[i].UnidadID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(unidad.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "unidad_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ContratoQuery) loadCargos(ctx context.Context, query *CargoQuery, nodes []*Contrato, init func(*Contrato), assign func(*Contrato, *Cargo)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Contrato)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(cargo.FieldContratoID)
	}
	query.Where(predicate.Cargo(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(contrato.CargosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ContratoID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "contrato_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ContratoQuery) loadPagos(ctx context.Context, query *PagoQuery, nodes []*Contrato, init func(*Contrato), assign func(*Contrato, *Pago)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Contrato)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pago.FieldContratoID)
	}
	query.Where(predicate.Pago(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(contrato.PagosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ContratoID
		if fk == nil {
			return fmt.Errorf(`foreign-key "contrato_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "contrato_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ContratoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ContratoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contrato.Table, contrato.Columns, sqlgraph.NewFieldSpec(contrato.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contrato.FieldID)
		for i := range fields {
			if fields[i] != contrato.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withEmpresa != nil {
			_spec.Node.AddColumnOnce(contrato.FieldEmpresaID)
		}
		if _q.withCliente != nil {
			_spec.Node.AddColumnOnce(contrato.FieldClienteID)
		}
		if _q.withUnidad != nil {
			_spec.Node.AddColumnOnce(contrato.FieldUnidadID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ContratoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(contrato.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = contrato.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContratoGroupBy is the group-by builder for Contrato entities.
type ContratoGroupBy struct {
	selector
	build *ContratoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ContratoGroupBy) Aggregate(fns ...AggregateFunc) *ContratoGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ContratoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContratoQuery, *ContratoGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ContratoGroupBy) sqlScan(ctx context.Context, root *ContratoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContratoSelect is the builder for selecting fields of Contrato entities.
type ContratoSelect struct {
	*ContratoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ContratoSelect) Aggregate(fns ...AggregateFunc) *ContratoSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ContratoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContratoQuery, *ContratoSelect](ctx, _s.ContratoQuery, _s, _s.inters, v)
}

func (_s *ContratoSelect) sqlScan(ctx context.Context, root *ContratoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
