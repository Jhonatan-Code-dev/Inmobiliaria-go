// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"rentals-go/ent/cliente"
	"rentals-go/ent/contrato"
	"rentals-go/ent/empresa"
	"rentals-go/ent/empresausuario"
	"rentals-go/ent/gasto"
	"rentals-go/ent/movimientocaja"
	"rentals-go/ent/pago"
	"rentals-go/ent/predicate"
	"rentals-go/ent/propiedad"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EmpresaQuery is the builder for querying Empresa entities.
type EmpresaQuery struct {
	config
	ctx                 *QueryContext
	order               []empresa.OrderOption
	inters              []Interceptor
	predicates          []predicate.Empresa
	withUsuariosEmpresa *EmpresaUsuarioQuery
	withClientes        *ClienteQuery
	withPropiedades     *PropiedadQuery
	withContratos       *ContratoQuery
	withPagos           *PagoQuery
	withGastos          *GastoQuery
	withMovimientosCaja *MovimientoCajaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmpresaQuery builder.
func (_q *EmpresaQuery) Where(ps ...predicate.Empresa) *EmpresaQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *EmpresaQuery) Limit(limit int) *EmpresaQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *EmpresaQuery) Offset(offset int) *EmpresaQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *EmpresaQuery) Unique(unique bool) *EmpresaQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *EmpresaQuery) Order(o ...empresa.OrderOption) *EmpresaQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUsuariosEmpresa chains the current query on the "usuarios_empresa" edge.
func (_q *EmpresaQuery) QueryUsuariosEmpresa() *EmpresaUsuarioQuery {
	query := (&EmpresaUsuarioClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(empresausuario.Table, empresausuario.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.UsuariosEmpresaTable, empresa.UsuariosEmpresaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryClientes chains the current query on the "clientes" edge.
func (_q *EmpresaQuery) QueryClientes() *ClienteQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(cliente.Table, cliente.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.ClientesTable, empresa.ClientesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPropiedades chains the current query on the "propiedades" edge.
func (_q *EmpresaQuery) QueryPropiedades() *PropiedadQuery {
	query := (&PropiedadClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(propiedad.Table, propiedad.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.PropiedadesTable, empresa.PropiedadesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryContratos chains the current query on the "contratos" edge.
func (_q *EmpresaQuery) QueryContratos() *ContratoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(contrato.Table, contrato.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.ContratosTable, empresa.ContratosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPagos chains the current query on the "pagos" edge.
func (_q *EmpresaQuery) QueryPagos() *PagoQuery {
	query := (&PagoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(pago.Table, pago.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.PagosTable, empresa.PagosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGastos chains the current query on the "gastos" edge.
func (_q *EmpresaQuery) QueryGastos() *GastoQuery {
	query := (&GastoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(gasto.Table, gasto.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.GastosTable, empresa.GastosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMovimientosCaja chains the current query on the "movimientos_caja" edge.
func (_q *EmpresaQuery) QueryMovimientosCaja() *MovimientoCajaQuery {
	query := (&MovimientoCajaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(empresa.Table, empresa.FieldID, selector),
			sqlgraph.To(movimientocaja.Table, movimientocaja.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, empresa.MovimientosCajaTable, empresa.MovimientosCajaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Empresa entity from the query.
// Returns a *NotFoundError when no Empresa was found.
func (_q *EmpresaQuery) First(ctx context.Context) (*Empresa, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{empresa.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *EmpresaQuery) FirstX(ctx context.Context) *Empresa {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Empresa ID from the query.
// Returns a *NotFoundError when no Empresa ID was found.
func (_q *EmpresaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{empresa.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *EmpresaQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Empresa entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Empresa entity is found.
// Returns a *NotFoundError when no Empresa entities are found.
func (_q *EmpresaQuery) Only(ctx context.Context) (*Empresa, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{empresa.Label}
	default:
		return nil, &NotSingularError{empresa.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *EmpresaQuery) OnlyX(ctx context.Context) *Empresa {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Empresa ID in the query.
// Returns a *NotSingularError when more than one Empresa ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *EmpresaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{empresa.Label}
	default:
		err = &NotSingularError{empresa.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *EmpresaQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Empresas.
func (_q *EmpresaQuery) All(ctx context.Context) ([]*Empresa, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Empresa, *EmpresaQuery]()
	return withInterceptors[[]*Empresa](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *EmpresaQuery) AllX(ctx context.Context) []*Empresa {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Empresa IDs.
func (_q *EmpresaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(empresa.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *EmpresaQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *EmpresaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*EmpresaQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *EmpresaQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *EmpresaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *EmpresaQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmpresaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *EmpresaQuery) Clone() *EmpresaQuery {
	if _q == nil {
		return nil
	}
	return &EmpresaQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]empresa.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Empresa{}, _q.predicates...),
		withUsuariosEmpresa: _q.withUsuariosEmpresa.Clone(),
		withClientes:        _q.withClientes.Clone(),
		withPropiedades:     _q.withPropiedades.Clone(),
		withContratos:       _q.withContratos.Clone(),
		withPagos:           _q.withPagos.Clone(),
		withGastos:          _q.withGastos.Clone(),
		withMovimientosCaja: _q.withMovimientosCaja.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUsuariosEmpresa tells the query-builder to eager-load the nodes that are connected to
// the "usuarios_empresa" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithUsuariosEmpresa(opts ...func(*EmpresaUsuarioQuery)) *EmpresaQuery {
	query := (&EmpresaUsuarioClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUsuariosEmpresa = query
	return _q
}

// WithClientes tells the query-builder to eager-load the nodes that are connected to
// the "clientes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithClientes(opts ...func(*ClienteQuery)) *EmpresaQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClientes = query
	return _q
}

// WithPropiedades tells the query-builder to eager-load the nodes that are connected to
// the "propiedades" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithPropiedades(opts ...func(*PropiedadQuery)) *EmpresaQuery {
	query := (&PropiedadClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPropiedades = query
	return _q
}

// WithContratos tells the query-builder to eager-load the nodes that are connected to
// the "contratos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithContratos(opts ...func(*ContratoQuery)) *EmpresaQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContratos = query
	return _q
}

// WithPagos tells the query-builder to eager-load the nodes that are connected to
// the "pagos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithPagos(opts ...func(*PagoQuery)) *EmpresaQuery {
	query := (&PagoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPagos = query
	return _q
}

// WithGastos tells the query-builder to eager-load the nodes that are connected to
// the "gastos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithGastos(opts ...func(*GastoQuery)) *EmpresaQuery {
	query := (&GastoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGastos = query
	return _q
}

// WithMovimientosCaja tells the query-builder to eager-load the nodes that are connected to
// the "movimientos_caja" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *EmpresaQuery) WithMovimientosCaja(opts ...func(*MovimientoCajaQuery)) *EmpresaQuery {
	query := (&MovimientoCajaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMovimientosCaja = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Empresa.Query().
//		GroupBy(empresa.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *EmpresaQuery) GroupBy(field string, fields ...string) *EmpresaGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmpresaGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = empresa.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.Empresa.Query().
//		Select(empresa.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *EmpresaQuery) Select(fields ...string) *EmpresaSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &EmpresaSelect{EmpresaQuery: _q}
	sbuild.label = empresa.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmpresaSelect configured with the given aggregations.
func (_q *EmpresaQuery) Aggregate(fns ...AggregateFunc) *EmpresaSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *EmpresaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !empresa.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *EmpresaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Empresa, error) {
	var (
		nodes       = []*Empresa{}
		_spec       = _q.querySpec()
		loadedTypes = [7]bool{
			_q.withUsuariosEmpresa != nil,
			_q.withClientes != nil,
			_q.withPropiedades != nil,
			_q.withContratos != nil,
			_q.withPagos != nil,
			_q.withGastos != nil,
			_q.withMovimientosCaja != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Empresa).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Empresa{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUsuariosEmpresa; query != nil {
		if err := _q.loadUsuariosEmpresa(ctx, query, nodes,
			func(n *Empresa) { n.Edges.UsuariosEmpresa = []*EmpresaUsuario{} },
			func(n *Empresa, e *EmpresaUsuario) { n.Edges.UsuariosEmpresa = append(n.Edges.UsuariosEmpresa, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withClientes; query != nil {
		if err := _q.loadClientes(ctx, query, nodes,
			func(n *Empresa) { n.Edges.Clientes = []*Cliente{} },
			func(n *Empresa, e *Cliente) { n.Edges.Clientes = append(n.Edges.Clientes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPropiedades; query != nil {
		if err := _q.loadPropiedades(ctx, query, nodes,
			func(n *Empresa) { n.Edges.Propiedades = []*Propiedad{} },
			func(n *Empresa, e *Propiedad) { n.Edges.Propiedades = append(n.Edges.Propiedades, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withContratos; query != nil {
		if err := _q.loadContratos(ctx, query, nodes,
			func(n *Empresa) { n.Edges.Contratos = []*Contrato{} },
			func(n *Empresa, e *Contrato) { n.Edges.Contratos = append(n.Edges.Contratos, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPagos; query != nil {
		if err := _q.loadPagos(ctx, query, nodes,
			func(n *Empresa) { n.Edges.Pagos = []*Pago{} },
			func(n *Empresa, e *Pago) { n.Edges.Pagos = append(n.Edges.Pagos, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGastos; query != nil {
		if err := _q.loadGastos(ctx, query, nodes,
			func(n *Empresa) { n.Edges.Gastos = []*Gasto{} },
			func(n *Empresa, e *Gasto) { n.Edges.Gastos = append(n.Edges.Gastos, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMovimientosCaja; query != nil {
		if err := _q.loadMovimientosCaja(ctx, query, nodes,
			func(n *Empresa) { n.Edges.MovimientosCaja = []*MovimientoCaja{} },
			func(n *Empresa, e *MovimientoCaja) { n.Edges.MovimientosCaja = append(n.Edges.MovimientosCaja, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *EmpresaQuery) loadUsuariosEmpresa(ctx context.Context, query *EmpresaUsuarioQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *EmpresaUsuario)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(empresausuario.FieldEmpresaID)
	}
	query.Where(predicate.EmpresaUsuario(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.UsuariosEmpresaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadClientes(ctx context.Context, query *ClienteQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *Cliente)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(cliente.FieldEmpresaID)
	}
	query.Where(predicate.Cliente(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.ClientesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadPropiedades(ctx context.Context, query *PropiedadQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *Propiedad)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(propiedad.FieldEmpresaID)
	}
	query.Where(predicate.Propiedad(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.PropiedadesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadContratos(ctx context.Context, query *ContratoQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *Contrato)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(contrato.FieldEmpresaID)
	}
	query.Where(predicate.Contrato(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.ContratosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadPagos(ctx context.Context, query *PagoQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *Pago)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pago.FieldEmpresaID)
	}
	query.Where(predicate.Pago(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.PagosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadGastos(ctx context.Context, query *GastoQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *Gasto)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(gasto.FieldEmpresaID)
	}
	query.Where(predicate.Gasto(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.GastosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *EmpresaQuery) loadMovimientosCaja(ctx context.Context, query *MovimientoCajaQuery, nodes []*Empresa, init func(*Empresa), assign func(*Empresa, *MovimientoCaja)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Empresa)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(movimientocaja.FieldEmpresaID)
	}
	query.Where(predicate.MovimientoCaja(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(empresa.MovimientosCajaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.EmpresaID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "empresa_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *EmpresaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *EmpresaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(empresa.Table, empresa.Columns, sqlgraph.NewFieldSpec(empresa.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, empresa.FieldID)
		for i := range fields {
			if fields[i] != empresa.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *EmpresaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(empresa.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = empresa.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmpresaGroupBy is the group-by builder for Empresa entities.
type EmpresaGroupBy struct {
	selector
	build *EmpresaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *EmpresaGroupBy) Aggregate(fns ...AggregateFunc) *EmpresaGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *EmpresaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmpresaQuery, *EmpresaGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *EmpresaGroupBy) sqlScan(ctx context.Context, root *EmpresaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmpresaSelect is the builder for selecting fields of Empresa entities.
type EmpresaSelect struct {
	*EmpresaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *EmpresaSelect) Aggregate(fns ...AggregateFunc) *EmpresaSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *EmpresaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmpresaQuery, *EmpresaSelect](ctx, _s.EmpresaQuery, _s, _s.inters, v)
}

func (_s *EmpresaSelect) sqlScan(ctx context.Context, root *EmpresaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
