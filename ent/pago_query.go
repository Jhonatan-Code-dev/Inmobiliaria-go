// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"rentals-go/ent/cliente"
	"rentals-go/ent/contrato"
	"rentals-go/ent/empresa"
	"rentals-go/ent/movimientocaja"
	"rentals-go/ent/pago"
	"rentals-go/ent/pagoaplicacion"
	"rentals-go/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PagoQuery is the builder for querying Pago entities.
type PagoQuery struct {
	config
	ctx                 *QueryContext
	order               []pago.OrderOption
	inters              []Interceptor
	predicates          []predicate.Pago
	withEmpresa         *EmpresaQuery
	withCliente         *ClienteQuery
	withContrato        *ContratoQuery
	withAplicaciones    *PagoAplicacionQuery
	withMovimientosCaja *MovimientoCajaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PagoQuery builder.
func (_q *PagoQuery) Where(ps ...predicate.Pago) *PagoQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PagoQuery) Limit(limit int) *PagoQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PagoQuery) Offset(offset int) *PagoQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PagoQuery) Unique(unique bool) *PagoQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PagoQuery) Order(o ...pago.OrderOption) *PagoQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryEmpresa chains the current query on the "empresa" edge.
func (_q *PagoQuery) QueryEmpresa() *EmpresaQuery {
	query := (&EmpresaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pago.Table, pago.FieldID, selector),
			sqlgraph.To(empresa.Table, empresa.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pago.EmpresaTable, pago.EmpresaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCliente chains the current query on the "cliente" edge.
func (_q *PagoQuery) QueryCliente() *ClienteQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pago.Table, pago.FieldID, selector),
			sqlgraph.To(cliente.Table, cliente.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pago.ClienteTable, pago.ClienteColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryContrato chains the current query on the "contrato" edge.
func (_q *PagoQuery) QueryContrato() *ContratoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pago.Table, pago.FieldID, selector),
			sqlgraph.To(contrato.Table, contrato.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pago.ContratoTable, pago.ContratoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAplicaciones chains the current query on the "aplicaciones" edge.
func (_q *PagoQuery) QueryAplicaciones() *PagoAplicacionQuery {
	query := (&PagoAplicacionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pago.Table, pago.FieldID, selector),
			sqlgraph.To(pagoaplicacion.Table, pagoaplicacion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, pago.AplicacionesTable, pago.AplicacionesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMovimientosCaja chains the current query on the "movimientos_caja" edge.
func (_q *PagoQuery) QueryMovimientosCaja() *MovimientoCajaQuery {
	query := (&MovimientoCajaClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pago.Table, pago.FieldID, selector),
			sqlgraph.To(movimientocaja.Table, movimientocaja.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, pago.MovimientosCajaTable, pago.MovimientosCajaColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Pago entity from the query.
// Returns a *NotFoundError when no Pago was found.
func (_q *PagoQuery) First(ctx context.Context) (*Pago, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pago.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PagoQuery) FirstX(ctx context.Context) *Pago {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Pago ID from the query.
// Returns a *NotFoundError when no Pago ID was found.
func (_q *PagoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pago.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PagoQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Pago entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Pago entity is found.
// Returns a *NotFoundError when no Pago entities are found.
func (_q *PagoQuery) Only(ctx context.Context) (*Pago, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pago.Label}
	default:
		return nil, &NotSingularError{pago.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PagoQuery) OnlyX(ctx context.Context) *Pago {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Pago ID in the query.
// Returns a *NotSingularError when more than one Pago ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PagoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pago.Label}
	default:
		err = &NotSingularError{pago.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PagoQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Pagos.
func (_q *PagoQuery) All(ctx context.Context) ([]*Pago, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Pago, *PagoQuery]()
	return withInterceptors[[]*Pago](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PagoQuery) AllX(ctx context.Context) []*Pago {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Pago IDs.
func (_q *PagoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(pago.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PagoQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PagoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PagoQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PagoQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PagoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PagoQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PagoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PagoQuery) Clone() *PagoQuery {
	if _q == nil {
		return nil
	}
	return &PagoQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]pago.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.Pago{}, _q.predicates...),
		withEmpresa:         _q.withEmpresa.Clone(),
		withCliente:         _q.withCliente.Clone(),
		withContrato:        _q.withContrato.Clone(),
		withAplicaciones:    _q.withAplicaciones.Clone(),
		withMovimientosCaja: _q.withMovimientosCaja.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithEmpresa tells the query-builder to eager-load the nodes that are connected to
// the "empresa" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoQuery) WithEmpresa(opts ...func(*EmpresaQuery)) *PagoQuery {
	query := (&EmpresaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEmpresa = query
	return _q
}

// WithCliente tells the query-builder to eager-load the nodes that are connected to
// the "cliente" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoQuery) WithCliente(opts ...func(*ClienteQuery)) *PagoQuery {
	query := (&ClienteClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCliente = query
	return _q
}

// WithContrato tells the query-builder to eager-load the nodes that are connected to
// the "contrato" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoQuery) WithContrato(opts ...func(*ContratoQuery)) *PagoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContrato = query
	return _q
}

// WithAplicaciones tells the query-builder to eager-load the nodes that are connected to
// the "aplicaciones" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoQuery) WithAplicaciones(opts ...func(*PagoAplicacionQuery)) *PagoQuery {
	query := (&PagoAplicacionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAplicaciones = query
	return _q
}

// WithMovimientosCaja tells the query-builder to eager-load the nodes that are connected to
// the "movimientos_caja" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoQuery) WithMovimientosCaja(opts ...func(*MovimientoCajaQuery)) *PagoQuery {
	query := (&MovimientoCajaClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMovimientosCaja = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Pago.Query().
//		GroupBy(pago.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PagoQuery) GroupBy(field string, fields ...string) *PagoGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PagoGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = pago.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.Pago.Query().
//		Select(pago.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *PagoQuery) Select(fields ...string) *PagoSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PagoSelect{PagoQuery: _q}
	sbuild.label = pago.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PagoSelect configured with the given aggregations.
func (_q *PagoQuery) Aggregate(fns ...AggregateFunc) *PagoSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PagoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !pago.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PagoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Pago, error) {
	var (
		nodes       = []*Pago{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withEmpresa != nil,
			_q.withCliente != nil,
			_q.withContrato != nil,
			_q.withAplicaciones != nil,
			_q.withMovimientosCaja != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Pago).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Pago{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withEmpresa; query != nil {
		if err := _q.loadEmpresa(ctx, query, nodes, nil,
			func(n *Pago, e *Empresa) { n.Edges.Empresa = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCliente; query != nil {
		if err := _q.loadCliente(ctx, query, nodes, nil,
			func(n *Pago, e *Cliente) { n.Edges.Cliente = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withContrato; query != nil {
		if err := _q.loadContrato(ctx, query, nodes, nil,
			func(n *Pago, e *Contrato) { n.Edges.Contrato = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAplicaciones; query != nil {
		if err := _q.loadAplicaciones(ctx, query, nodes,
			func(n *Pago) { n.Edges.Aplicaciones = []*PagoAplicacion{} },
			func(n *Pago, e *PagoAplicacion) { n.Edges.Aplicaciones = append(n.Edges.Aplicaciones, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMovimientosCaja; query != nil {
		if err := _q.loadMovimientosCaja(ctx, query, nodes,
			func(n *Pago) { n.Edges.MovimientosCaja = []*MovimientoCaja{} },
			func(n *Pago, e *MovimientoCaja) { n.Edges.MovimientosCaja = append(n.Edges.MovimientosCaja, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PagoQuery) loadEmpresa(ctx context.Context, query *EmpresaQuery, nodes []*Pago, init func(*Pago), assign func(*Pago, *Empresa)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Pago)
	for i := range nodes {
		fk := nodes[i].EmpresaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(empresa.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "empresa_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PagoQuery) loadCliente(ctx context.Context, query *ClienteQuery, nodes []*Pago, init func(*Pago), assign func(*Pago, *Cliente)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Pago)
	for i := range nodes {
		if nodes[i].ClienteID == nil {
			continue
		}
		fk := *nodes[i].ClienteID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(cliente.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "cliente_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PagoQuery) loadContrato(ctx context.Context, query *ContratoQuery, nodes []*Pago, init func(*Pago), assign func(*Pago, *Contrato)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Pago)
	for i := range nodes {
		if nodes[i].ContratoID == nil {
			continue
		}
		fk := *nodes[i].ContratoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(contrato.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "contrato_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PagoQuery) loadAplicaciones(ctx context.Context, query *PagoAplicacionQuery, nodes []*Pago, init func(*Pago), assign func(*Pago, *PagoAplicacion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Pago)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(pagoaplicacion.FieldPagoID)
	}
	query.Where(predicate.PagoAplicacion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(pago.AplicacionesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PagoID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pago_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *PagoQuery) loadMovimientosCaja(ctx context.Context, query *MovimientoCajaQuery, nodes []*Pago, init func(*Pago), assign func(*Pago, *MovimientoCaja)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Pago)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(movimientocaja.FieldPagoID)
	}
	query.Where(predicate.MovimientoCaja(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(pago.MovimientosCajaColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PagoID
		if fk == nil {
			return fmt.Errorf(`foreign-key "pago_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "pago_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *PagoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PagoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pago.Table, pago.Columns, sqlgraph.NewFieldSpec(pago.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pago.FieldID)
		for i := range fields {
			if fields[i] != pago.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withEmpresa != nil {
			_spec.Node.AddColumnOnce(pago.FieldEmpresaID)
		}
		if _q.withCliente != nil {
			_spec.Node.AddColumnOnce(pago.FieldClienteID)
		}
		if _q.withContrato != nil {
			_spec.Node.AddColumnOnce(pago.FieldContratoID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PagoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(pago.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = pago.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PagoGroupBy is the group-by builder for Pago entities.
type PagoGroupBy struct {
	selector
	build *PagoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PagoGroupBy) Aggregate(fns ...AggregateFunc) *PagoGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PagoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PagoQuery, *PagoGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PagoGroupBy) sqlScan(ctx context.Context, root *PagoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PagoSelect is the builder for selecting fields of Pago entities.
type PagoSelect struct {
	*PagoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PagoSelect) Aggregate(fns ...AggregateFunc) *PagoSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PagoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PagoQuery, *PagoSelect](ctx, _s.PagoQuery, _s, _s.inters, v)
}

func (_s *PagoSelect) sqlScan(ctx context.Context, root *PagoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
