// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"rentals-go/ent/cargo"
	"rentals-go/ent/pago"
	"rentals-go/ent/pagoaplicacion"
	"rentals-go/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PagoAplicacionQuery is the builder for querying PagoAplicacion entities.
type PagoAplicacionQuery struct {
	config
	ctx        *QueryContext
	order      []pagoaplicacion.OrderOption
	inters     []Interceptor
	predicates []predicate.PagoAplicacion
	withPago   *PagoQuery
	withCargo  *CargoQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PagoAplicacionQuery builder.
func (_q *PagoAplicacionQuery) Where(ps ...predicate.PagoAplicacion) *PagoAplicacionQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PagoAplicacionQuery) Limit(limit int) *PagoAplicacionQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PagoAplicacionQuery) Offset(offset int) *PagoAplicacionQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PagoAplicacionQuery) Unique(unique bool) *PagoAplicacionQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PagoAplicacionQuery) Order(o ...pagoaplicacion.OrderOption) *PagoAplicacionQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryPago chains the current query on the "pago" edge.
func (_q *PagoAplicacionQuery) QueryPago() *PagoQuery {
	query := (&PagoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pagoaplicacion.Table, pagoaplicacion.FieldID, selector),
			sqlgraph.To(pago.Table, pago.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pagoaplicacion.PagoTable, pagoaplicacion.PagoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCargo chains the current query on the "cargo" edge.
func (_q *PagoAplicacionQuery) QueryCargo() *CargoQuery {
	query := (&CargoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pagoaplicacion.Table, pagoaplicacion.FieldID, selector),
			sqlgraph.To(cargo.Table, cargo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pagoaplicacion.CargoTable, pagoaplicacion.CargoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PagoAplicacion entity from the query.
// Returns a *NotFoundError when no PagoAplicacion was found.
func (_q *PagoAplicacionQuery) First(ctx context.Context) (*PagoAplicacion, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pagoaplicacion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PagoAplicacionQuery) FirstX(ctx context.Context) *PagoAplicacion {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PagoAplicacion ID from the query.
// Returns a *NotFoundError when no PagoAplicacion ID was found.
func (_q *PagoAplicacionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pagoaplicacion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PagoAplicacionQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PagoAplicacion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PagoAplicacion entity is found.
// Returns a *NotFoundError when no PagoAplicacion entities are found.
func (_q *PagoAplicacionQuery) Only(ctx context.Context) (*PagoAplicacion, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pagoaplicacion.Label}
	default:
		return nil, &NotSingularError{pagoaplicacion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PagoAplicacionQuery) OnlyX(ctx context.Context) *PagoAplicacion {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PagoAplicacion ID in the query.
// Returns a *NotSingularError when more than one PagoAplicacion ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PagoAplicacionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pagoaplicacion.Label}
	default:
		err = &NotSingularError{pagoaplicacion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PagoAplicacionQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PagoAplicacions.
func (_q *PagoAplicacionQuery) All(ctx context.Context) ([]*PagoAplicacion, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PagoAplicacion, *PagoAplicacionQuery]()
	return withInterceptors[[]*PagoAplicacion](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PagoAplicacionQuery) AllX(ctx context.Context) []*PagoAplicacion {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PagoAplicacion IDs.
func (_q *PagoAplicacionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(pagoaplicacion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PagoAplicacionQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PagoAplicacionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PagoAplicacionQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PagoAplicacionQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PagoAplicacionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PagoAplicacionQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PagoAplicacionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PagoAplicacionQuery) Clone() *PagoAplicacionQuery {
	if _q == nil {
		return nil
	}
	return &PagoAplicacionQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]pagoaplicacion.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.PagoAplicacion{}, _q.predicates...),
		withPago:   _q.withPago.Clone(),
		withCargo:  _q.withCargo.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithPago tells the query-builder to eager-load the nodes that are connected to
// the "pago" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoAplicacionQuery) WithPago(opts ...func(*PagoQuery)) *PagoAplicacionQuery {
	query := (&PagoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPago = query
	return _q
}

// WithCargo tells the query-builder to eager-load the nodes that are connected to
// the "cargo" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *PagoAplicacionQuery) WithCargo(opts ...func(*CargoQuery)) *PagoAplicacionQuery {
	query := (&CargoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCargo = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PagoAplicacion.Query().
//		GroupBy(pagoaplicacion.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PagoAplicacionQuery) GroupBy(field string, fields ...string) *PagoAplicacionGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PagoAplicacionGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = pagoaplicacion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.PagoAplicacion.Query().
//		Select(pagoaplicacion.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *PagoAplicacionQuery) Select(fields ...string) *PagoAplicacionSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PagoAplicacionSelect{PagoAplicacionQuery: _q}
	sbuild.label = pagoaplicacion.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PagoAplicacionSelect configured with the given aggregations.
func (_q *PagoAplicacionQuery) Aggregate(fns ...AggregateFunc) *PagoAplicacionSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PagoAplicacionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !pagoaplicacion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PagoAplicacionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PagoAplicacion, error) {
	var (
		nodes       = []*PagoAplicacion{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withPago != nil,
			_q.withCargo != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PagoAplicacion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PagoAplicacion{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withPago; query != nil {
		if err := _q.loadPago(ctx, query, nodes, nil,
			func(n *PagoAplicacion, e *Pago) { n.Edges.Pago = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCargo; query != nil {
		if err := _q.loadCargo(ctx, query, nodes, nil,
			func(n *PagoAplicacion, e *Cargo) { n.Edges.Cargo = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *PagoAplicacionQuery) loadPago(ctx context.Context, query *PagoQuery, nodes []*PagoAplicacion, init func(*PagoAplicacion), assign func(*PagoAplicacion, *Pago)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PagoAplicacion)
	for i := range nodes {
		fk := nodes[i].PagoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(pago.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "pago_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *PagoAplicacionQuery) loadCargo(ctx context.Context, query *CargoQuery, nodes []*PagoAplicacion, init func(*PagoAplicacion), assign func(*PagoAplicacion, *Cargo)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PagoAplicacion)
	for i := range nodes {
		fk := nodes[i].CargoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(cargo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "cargo_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *PagoAplicacionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PagoAplicacionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pagoaplicacion.Table, pagoaplicacion.Columns, sqlgraph.NewFieldSpec(pagoaplicacion.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pagoaplicacion.FieldID)
		for i := range fields {
			if fields[i] != pagoaplicacion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withPago != nil {
			_spec.Node.AddColumnOnce(pagoaplicacion.FieldPagoID)
		}
		if _q.withCargo != nil {
			_spec.Node.AddColumnOnce(pagoaplicacion.FieldCargoID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PagoAplicacionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(pagoaplicacion.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = pagoaplicacion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PagoAplicacionGroupBy is the group-by builder for PagoAplicacion entities.
type PagoAplicacionGroupBy struct {
	selector
	build *PagoAplicacionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PagoAplicacionGroupBy) Aggregate(fns ...AggregateFunc) *PagoAplicacionGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PagoAplicacionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PagoAplicacionQuery, *PagoAplicacionGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PagoAplicacionGroupBy) sqlScan(ctx context.Context, root *PagoAplicacionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PagoAplicacionSelect is the builder for selecting fields of PagoAplicacion entities.
type PagoAplicacionSelect struct {
	*PagoAplicacionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PagoAplicacionSelect) Aggregate(fns ...AggregateFunc) *PagoAplicacionSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PagoAplicacionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PagoAplicacionQuery, *PagoAplicacionSelect](ctx, _s.PagoAplicacionQuery, _s, _s.inters, v)
}

func (_s *PagoAplicacionSelect) sqlScan(ctx context.Context, root *PagoAplicacionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
