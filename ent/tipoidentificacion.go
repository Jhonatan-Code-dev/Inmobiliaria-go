// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"rentals-go/ent/tipoidentificacion"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TipoIdentificacion is the model entity for the TipoIdentificacion schema.
type TipoIdentificacion struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreadoEn holds the value of the "creado_en" field.
	CreadoEn time.Time `json:"creado_en,omitempty"`
	// ActualizadoEn holds the value of the "actualizado_en" field.
	ActualizadoEn time.Time `json:"actualizado_en,omitempty"`
	// Codigo holds the value of the "codigo" field.
	Codigo string `json:"codigo,omitempty"`
	// Nombre holds the value of the "nombre" field.
	Nombre string `json:"nombre,omitempty"`
	// Pais holds the value of the "pais" field.
	Pais *string `json:"pais,omitempty"`
	// Activo holds the value of the "activo" field.
	Activo bool `json:"activo,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TipoIdentificacionQuery when eager-loading is set.
	Edges        TipoIdentificacionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TipoIdentificacionEdges holds the relations/edges for other nodes in the graph.
type TipoIdentificacionEdges struct {
	// Clientes holds the value of the clientes edge.
	Clientes []*Cliente `json:"clientes,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ClientesOrErr returns the Clientes value or an error if the edge
// was not loaded in eager-loading.
func (e TipoIdentificacionEdges) ClientesOrErr() ([]*Cliente, error) {
	if e.loadedTypes[0] {
		return e.Clientes, nil
	}
	return nil, &NotLoadedError{edge: "clientes"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TipoIdentificacion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tipoidentificacion.FieldActivo:
			values[i] = new(sql.NullBool)
		case tipoidentificacion.FieldID:
			values[i] = new(sql.NullInt64)
		case tipoidentificacion.FieldCodigo, tipoidentificacion.FieldNombre, tipoidentificacion.FieldPais:
			values[i] = new(sql.NullString)
		case tipoidentificacion.FieldCreadoEn, tipoidentificacion.FieldActualizadoEn:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TipoIdentificacion fields.
func (_m *TipoIdentificacion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tipoidentificacion.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case tipoidentificacion.FieldCreadoEn:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field creado_en", values[i])
			} else if value.Valid {
				_m.CreadoEn = value.Time
			}
		case tipoidentificacion.FieldActualizadoEn:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field actualizado_en", values[i])
			} else if value.Valid {
				_m.ActualizadoEn = value.Time
			}
		case tipoidentificacion.FieldCodigo:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field codigo", values[i])
			} else if value.Valid {
				_m.Codigo = value.String
			}
		case tipoidentificacion.FieldNombre:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field nombre", values[i])
			} else if value.Valid {
				_m.Nombre = value.String
			}
		case tipoidentificacion.FieldPais:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field pais", values[i])
			} else if value.Valid {
				_m.Pais = new(string)
				*_m.Pais = value.String
			}
		case tipoidentificacion.FieldActivo:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field activo", values[i])
			} else if value.Valid {
				_m.Activo = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TipoIdentificacion.
// This includes values selected through modifiers, order, etc.
func (_m *TipoIdentificacion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryClientes queries the "clientes" edge of the TipoIdentificacion entity.
func (_m *TipoIdentificacion) QueryClientes() *ClienteQuery {
	return NewTipoIdentificacionClient(_m.config).QueryClientes(_m)
}

// Update returns a builder for updating this TipoIdentificacion.
// Note that you need to call TipoIdentificacion.Unwrap() before calling this method if this TipoIdentificacion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TipoIdentificacion) Update() *TipoIdentificacionUpdateOne {
	return NewTipoIdentificacionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TipoIdentificacion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TipoIdentificacion) Unwrap() *TipoIdentificacion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TipoIdentificacion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TipoIdentificacion) String() string {
	var builder strings.Builder
	builder.WriteString("TipoIdentificacion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("creado_en=")
	builder.WriteString(_m.CreadoEn.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("actualizado_en=")
	builder.WriteString(_m.ActualizadoEn.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("codigo=")
	builder.WriteString(_m.Codigo)
	builder.WriteString(", ")
	builder.WriteString("nombre=")
	builder.WriteString(_m.Nombre)
	builder.WriteString(", ")
	if v := _m.Pais; v != nil {
		builder.WriteString("pais=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("activo=")
	builder.WriteString(fmt.Sprintf("%v", _m.Activo))
	builder.WriteByte(')')
	return builder.String()
}

// TipoIdentificacions is a parsable slice of TipoIdentificacion.
type TipoIdentificacions []*TipoIdentificacion
