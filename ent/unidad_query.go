// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"rentals-go/ent/contrato"
	"rentals-go/ent/predicate"
	"rentals-go/ent/propiedad"
	"rentals-go/ent/serviciomedicion"
	"rentals-go/ent/ticket"
	"rentals-go/ent/unidad"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UnidadQuery is the builder for querying Unidad entities.
type UnidadQuery struct {
	config
	ctx                    *QueryContext
	order                  []unidad.OrderOption
	inters                 []Interceptor
	predicates             []predicate.Unidad
	withPropiedad          *PropiedadQuery
	withContratos          *ContratoQuery
	withServicioMediciones *ServicioMedicionQuery
	withTickets            *TicketQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UnidadQuery builder.
func (_q *UnidadQuery) Where(ps ...predicate.Unidad) *UnidadQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *UnidadQuery) Limit(limit int) *UnidadQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *UnidadQuery) Offset(offset int) *UnidadQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *UnidadQuery) Unique(unique bool) *UnidadQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *UnidadQuery) Order(o ...unidad.OrderOption) *UnidadQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryPropiedad chains the current query on the "propiedad" edge.
func (_q *UnidadQuery) QueryPropiedad() *PropiedadQuery {
	query := (&PropiedadClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(unidad.Table, unidad.FieldID, selector),
			sqlgraph.To(propiedad.Table, propiedad.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, unidad.PropiedadTable, unidad.PropiedadColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryContratos chains the current query on the "contratos" edge.
func (_q *UnidadQuery) QueryContratos() *ContratoQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(unidad.Table, unidad.FieldID, selector),
			sqlgraph.To(contrato.Table, contrato.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, unidad.ContratosTable, unidad.ContratosColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryServicioMediciones chains the current query on the "servicio_mediciones" edge.
func (_q *UnidadQuery) QueryServicioMediciones() *ServicioMedicionQuery {
	query := (&ServicioMedicionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(unidad.Table, unidad.FieldID, selector),
			sqlgraph.To(serviciomedicion.Table, serviciomedicion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, unidad.ServicioMedicionesTable, unidad.ServicioMedicionesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTickets chains the current query on the "tickets" edge.
func (_q *UnidadQuery) QueryTickets() *TicketQuery {
	query := (&TicketClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(unidad.Table, unidad.FieldID, selector),
			sqlgraph.To(ticket.Table, ticket.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, unidad.TicketsTable, unidad.TicketsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Unidad entity from the query.
// Returns a *NotFoundError when no Unidad was found.
func (_q *UnidadQuery) First(ctx context.Context) (*Unidad, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{unidad.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *UnidadQuery) FirstX(ctx context.Context) *Unidad {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Unidad ID from the query.
// Returns a *NotFoundError when no Unidad ID was found.
func (_q *UnidadQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{unidad.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *UnidadQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Unidad entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Unidad entity is found.
// Returns a *NotFoundError when no Unidad entities are found.
func (_q *UnidadQuery) Only(ctx context.Context) (*Unidad, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{unidad.Label}
	default:
		return nil, &NotSingularError{unidad.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *UnidadQuery) OnlyX(ctx context.Context) *Unidad {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Unidad ID in the query.
// Returns a *NotSingularError when more than one Unidad ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *UnidadQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{unidad.Label}
	default:
		err = &NotSingularError{unidad.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *UnidadQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Unidads.
func (_q *UnidadQuery) All(ctx context.Context) ([]*Unidad, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Unidad, *UnidadQuery]()
	return withInterceptors[[]*Unidad](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *UnidadQuery) AllX(ctx context.Context) []*Unidad {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Unidad IDs.
func (_q *UnidadQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(unidad.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *UnidadQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *UnidadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*UnidadQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *UnidadQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *UnidadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *UnidadQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UnidadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *UnidadQuery) Clone() *UnidadQuery {
	if _q == nil {
		return nil
	}
	return &UnidadQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]unidad.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.Unidad{}, _q.predicates...),
		withPropiedad:          _q.withPropiedad.Clone(),
		withContratos:          _q.withContratos.Clone(),
		withServicioMediciones: _q.withServicioMediciones.Clone(),
		withTickets:            _q.withTickets.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithPropiedad tells the query-builder to eager-load the nodes that are connected to
// the "propiedad" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UnidadQuery) WithPropiedad(opts ...func(*PropiedadQuery)) *UnidadQuery {
	query := (&PropiedadClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPropiedad = query
	return _q
}

// WithContratos tells the query-builder to eager-load the nodes that are connected to
// the "contratos" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UnidadQuery) WithContratos(opts ...func(*ContratoQuery)) *UnidadQuery {
	query := (&ContratoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withContratos = query
	return _q
}

// WithServicioMediciones tells the query-builder to eager-load the nodes that are connected to
// the "servicio_mediciones" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UnidadQuery) WithServicioMediciones(opts ...func(*ServicioMedicionQuery)) *UnidadQuery {
	query := (&ServicioMedicionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withServicioMediciones = query
	return _q
}

// WithTickets tells the query-builder to eager-load the nodes that are connected to
// the "tickets" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *UnidadQuery) WithTickets(opts ...func(*TicketQuery)) *UnidadQuery {
	query := (&TicketClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTickets = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Unidad.Query().
//		GroupBy(unidad.FieldCreadoEn).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *UnidadQuery) GroupBy(field string, fields ...string) *UnidadGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UnidadGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = unidad.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreadoEn time.Time `json:"creado_en,omitempty"`
//	}
//
//	client.Unidad.Query().
//		Select(unidad.FieldCreadoEn).
//		Scan(ctx, &v)
func (_q *UnidadQuery) Select(fields ...string) *UnidadSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &UnidadSelect{UnidadQuery: _q}
	sbuild.label = unidad.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UnidadSelect configured with the given aggregations.
func (_q *UnidadQuery) Aggregate(fns ...AggregateFunc) *UnidadSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *UnidadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !unidad.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *UnidadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Unidad, error) {
	var (
		nodes       = []*Unidad{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withPropiedad != nil,
			_q.withContratos != nil,
			_q.withServicioMediciones != nil,
			_q.withTickets != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Unidad).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Unidad{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withPropiedad; query != nil {
		if err := _q.loadPropiedad(ctx, query, nodes, nil,
			func(n *Unidad, e *Propiedad) { n.Edges.Propiedad = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withContratos; query != nil {
		if err := _q.loadContratos(ctx, query, nodes,
			func(n *Unidad) { n.Edges.Contratos = []*Contrato{} },
			func(n *Unidad, e *Contrato) { n.Edges.Contratos = append(n.Edges.Contratos, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withServicioMediciones; query != nil {
		if err := _q.loadServicioMediciones(ctx, query, nodes,
			func(n *Unidad) { n.Edges.ServicioMediciones = []*ServicioMedicion{} },
			func(n *Unidad, e *ServicioMedicion) {
				n.Edges.ServicioMediciones = append(n.Edges.ServicioMediciones, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withTickets; query != nil {
		if err := _q.loadTickets(ctx, query, nodes,
			func(n *Unidad) { n.Edges.Tickets = []*Ticket{} },
			func(n *Unidad, e *Ticket) { n.Edges.Tickets = append(n.Edges.Tickets, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *UnidadQuery) loadPropiedad(ctx context.Context, query *PropiedadQuery, nodes []*Unidad, init func(*Unidad), assign func(*Unidad, *Propiedad)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Unidad)
	for i := range nodes {
		fk := nodes[i].PropiedadID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(propiedad.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "propiedad_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *UnidadQuery) loadContratos(ctx context.Context, query *ContratoQuery, nodes []*Unidad, init func(*Unidad), assign func(*Unidad, *Contrato)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Unidad)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(contrato.FieldUnidadID)
	}
	query.Where(predicate.Contrato(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(unidad.ContratosColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.UnidadID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "unidad_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *UnidadQuery) loadServicioMediciones(ctx context.Context, query *ServicioMedicionQuery, nodes []*Unidad, init func(*Unidad), assign func(*Unidad, *ServicioMedicion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Unidad)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(serviciomedicion.FieldUnidadID)
	}
	query.Where(predicate.ServicioMedicion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(unidad.ServicioMedicionesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.UnidadID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "unidad_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *UnidadQuery) loadTickets(ctx context.Context, query *TicketQuery, nodes []*Unidad, init func(*Unidad), assign func(*Unidad, *Ticket)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Unidad)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(ticket.FieldUnidadID)
	}
	query.Where(predicate.Ticket(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(unidad.TicketsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.UnidadID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "unidad_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *UnidadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *UnidadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(unidad.Table, unidad.Columns, sqlgraph.NewFieldSpec(unidad.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, unidad.FieldID)
		for i := range fields {
			if fields[i] != unidad.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withPropiedad != nil {
			_spec.Node.AddColumnOnce(unidad.FieldPropiedadID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *UnidadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(unidad.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = unidad.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UnidadGroupBy is the group-by builder for Unidad entities.
type UnidadGroupBy struct {
	selector
	build *UnidadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *UnidadGroupBy) Aggregate(fns ...AggregateFunc) *UnidadGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *UnidadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UnidadQuery, *UnidadGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *UnidadGroupBy) sqlScan(ctx context.Context, root *UnidadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UnidadSelect is the builder for selecting fields of Unidad entities.
type UnidadSelect struct {
	*UnidadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *UnidadSelect) Aggregate(fns ...AggregateFunc) *UnidadSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *UnidadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UnidadQuery, *UnidadSelect](ctx, _s.UnidadQuery, _s, _s.inters, v)
}

func (_s *UnidadSelect) sqlScan(ctx context.Context, root *UnidadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
